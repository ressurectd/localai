#!/bin/sh
# localai launcher for Git Bash / MSYS.
here="$(cd "$(dirname "$0")/.." && pwd)"
py="$here/.venv/Scripts/python.exe"
[ -x "$py" ] || py="$here/.venv/bin/python"
if [ ! -x "$py" ]; then
  echo "localai: virtualenv not found under $here/.venv" >&2
  exit 78
fi
exec "$py" -m localai "$@"
